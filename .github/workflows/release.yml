name: Build APK & Release

# Manually triggered: builds the debug APK and publishes it as a GitHub release.
on:
    workflow_dispatch:
        inputs:
            tag:
                description: "Release tag (e.g. v1.0)"
                required: true
                default: "v1.0"
            prerelease:
                description: "Mark as pre-release?"
                type: boolean
                default: false

permissions:
    contents: write

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Set up JDK 17
              uses: actions/setup-java@v4
              with:
                  distribution: temurin
                  java-version: "17"

            - name: Set up Android SDK
              uses: android-actions/setup-android@v3

            - name: Install SDK packages
              run: sdkmanager "platforms;android-33" "build-tools;35.0.0"

            - name: Decode signing keystore
              run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > "${{ github.workspace }}/release.jks"

            - name: Build signed release APK
              env:
                  RMSTOK_KEYSTORE: ${{ github.workspace }}/release.jks
                  RMSTOK_STORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
                  RMSTOK_KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
                  RMSTOK_KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
              run: |
                  chmod +x gradlew
                  ./gradlew assembleRelease -Pbtv=35.0.0 --no-daemon

            - name: Stage APK
              run: cp app/build/outputs/apk/release/app-release.apk "RMSTok-${{ inputs.tag }}.apk"

            - name: Clean up keystore
              if: always()
              run: rm -f "${{ github.workspace }}/release.jks"

            - name: Create release
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  gh release create "${{ inputs.tag }}" "RMSTok-${{ inputs.tag }}.apk" \
                    --title "RMSTok Android ${{ inputs.tag }}" \
                    --notes "RMSTok Android client. Sideload the APK and open it on TikTok." \
                    ${{ inputs.prerelease == true && '--prerelease' || '--latest' }}
